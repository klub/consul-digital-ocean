# .bash_profile

# Get the aliases and functions
if [ -f ~/.bashrc ]; then
	. ~/.bashrc
fi
. /home/{{ web_olympus_app.name }}/.venv

# User specific environment and startup programs

PATH=$PATH:$HOME/bin

export PATH
